import{_ as a}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as e,c as r,e as i}from"./app-AT5MTRLB.js";const h={},d=i('<h2 id="kafka" tabindex="-1"><a class="header-anchor" href="#kafka" aria-hidden="true">#</a> Kafka</h2><h3 id="不丢失" tabindex="-1"><a class="header-anchor" href="#不丢失" aria-hidden="true">#</a> 不丢失</h3><h4 id="消费者端" tabindex="-1"><a class="header-anchor" href="#消费者端" aria-hidden="true">#</a> 消费者端</h4><p>consumer端丢失消息的情形比较简单：如果在消息处理完成前就提交了offset，那么就有可能造成数据的丢失。由于Kafka consumer默认是自动提交位移的，所以在后台提交位移前一定要保证消息被正常处理了，因此不建议采用很重的处理逻辑，如果处理耗时很长，则建议把逻辑放到另一个线程中去做。为了避免数据丢失，可以采用手动提交offset：</p><ol><li><p>enable.auto.commit=false 关闭自动提交位移</p></li><li><p>在消息被完整处理之后再手动提交位移</p></li></ol><h4 id="生产者端" tabindex="-1"><a class="header-anchor" href="#生产者端" aria-hidden="true">#</a> 生产者端</h4><p>生产者丢失消息是最复杂的情形了。生产者(Producer) 使用 send 方法发送消息实际上是异步的操作，我们可以通过 get()方法获取调用结果，但是这样也让它变为了同步操作，但是一般不推荐这么做！可以采用为其<mark>添加回调函数</mark>的形式。这个回调函数会在 Producer 收到 ack 时调用，此处就和acks参数配置[1、0、-1]密切相关了。</p><p>如果消息发送失败的话，我们检查失败的原因之后重新发送即可！另外这里推荐为 Producer 的retries（重试次数），设置一个比较合理的值，一般是 3 ，但是为了保证消息不丢失的话一般会设置比较大一点。设置完成之后，当出现网络问题之后能够自动重试消息发送，避免消息丢失。另外，建议还要设置重试间隔retry.backoff.ms，默认是100ms，可以设置到1秒左右，因为间隔太小的话重试的效果就不明显了，网络波动一次，你3次一下子就重试完了。</p><h3 id="不重复" tabindex="-1"><a class="header-anchor" href="#不重复" aria-hidden="true">#</a> 不重复</h3><h3 id="顺序性" tabindex="-1"><a class="header-anchor" href="#顺序性" aria-hidden="true">#</a> 顺序性</h3><ul><li>分区策略<mark>（推荐）</mark>。Kafka的主题可以分为多个分区，每个分区内的消息是有序的。因此，首先要确保生产者将相关的消息发送到同一个分区。</li><li>消费者组（<mark>慎用</mark>）。多个消费者可以组成一个消费者组，并同时消费同一个主题的不同分区。当启动多个消费者时，Kafka会自动分配分区给不同的消费者，保证每个分区只由一个消费者处理。此时分区内有序，分组内不一定有序。</li><li>偏移量。每个分区中的消息都有一个唯一标识符，称为偏移量。消费者可以通过指定偏移量来读取分区中的特定消息。</li><li>配置消费者属性。在消费者端，可以通过设置一些属性来进一步确保消息的顺序消费。</li></ul><h2 id="不重复通用处理方案" tabindex="-1"><a class="header-anchor" href="#不重复通用处理方案" aria-hidden="true">#</a> 不重复通用处理方案</h2><h3 id="业务幂等判断和处理" tabindex="-1"><a class="header-anchor" href="#业务幂等判断和处理" aria-hidden="true">#</a> 业务幂等判断和处理</h3>',13),t=[d];function c(n,s){return e(),r("div",null,t)}const f=a(h,[["render",c],["__file","消息队列常见问题和解决方案.html.vue"]]);export{f as default};
